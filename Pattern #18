#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int n,t=69;
    cin>>n;
    for(int i=0;i<n;i++)
    {
      for(int j=t-i;j<=t;j++)
      {
        cout<<char(j);
      }cout<<endl;
    }
    
    
    
}
